[build-system]
requires = ["maturin>=0.13,<0.14"]
build-backend = "maturin"

[project]
name = "tantivy"
requires-python = ">=3.8"

[project.optional-dependencies]
dev = [
    "nox",
]

[tool.maturin]
bindings = "pyo3"
